import { AddNewProductRequest } from './add-new-product-request.model';

describe('AddNewProductRequest', () => {
  it('should create an instance', () => {
    expect(new AddNewProductRequest()).toBeTruthy();
  });
});
