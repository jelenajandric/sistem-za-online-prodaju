import { GetAllCategories } from './get-all-categories.model';

describe('GetAllCategories', () => {
  it('should create an instance', () => {
    expect(new GetAllCategories()).toBeTruthy();
  });
});
