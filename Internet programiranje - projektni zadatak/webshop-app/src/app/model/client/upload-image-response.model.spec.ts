import { UploadImageResponse } from './upload-image-response.model';

describe('UploadImageResponse', () => {
  it('should create an instance', () => {
    expect(new UploadImageResponse()).toBeTruthy();
  });
});
