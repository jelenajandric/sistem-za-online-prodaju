import { GetProductForUpdate } from './get-product-for-update.model';

describe('GetProductForUpdate', () => {
  it('should create an instance', () => {
    expect(new GetProductForUpdate()).toBeTruthy();
  });
});
