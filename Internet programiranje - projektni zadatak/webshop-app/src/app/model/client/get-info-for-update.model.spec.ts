import { GetInfoForUpdate } from './get-info-for-update.model';

describe('GetInfoForUpdate', () => {
  it('should create an instance', () => {
    expect(new GetInfoForUpdate()).toBeTruthy();
  });
});
