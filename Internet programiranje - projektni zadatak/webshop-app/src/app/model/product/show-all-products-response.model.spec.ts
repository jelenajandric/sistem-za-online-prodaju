import { ShowAllProductsResponse } from './show-all-products-response.model';

describe('ShowAllProductsResponse', () => {
  it('should create an instance', () => {
    expect(new ShowAllProductsResponse()).toBeTruthy();
  });
});
