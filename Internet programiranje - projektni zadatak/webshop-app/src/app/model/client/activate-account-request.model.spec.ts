import { ActivateAccountRequest } from './activate-account-request.model';

describe('ActivateAccountRequest', () => {
  it('should create an instance', () => {
    expect(new ActivateAccountRequest()).toBeTruthy();
  });
});
