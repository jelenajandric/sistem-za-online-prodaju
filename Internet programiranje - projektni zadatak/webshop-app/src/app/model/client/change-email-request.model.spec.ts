import { ChangeEmailRequest } from './change-email-request.model';

describe('ChangeEmailRequest', () => {
  it('should create an instance', () => {
    expect(new ChangeEmailRequest()).toBeTruthy();
  });
});
