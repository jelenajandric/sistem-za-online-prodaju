import { GetClientInfoResponse } from './get-client-info-response.model';

describe('GetClientInfoResponse', () => {
  it('should create an instance', () => {
    expect(new GetClientInfoResponse()).toBeTruthy();
  });
});
