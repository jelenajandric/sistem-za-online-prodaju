import { UpdateProductResponse } from './update-product-response.model';

describe('UpdateProductResponse', () => {
  it('should create an instance', () => {
    expect(new UpdateProductResponse()).toBeTruthy();
  });
});
