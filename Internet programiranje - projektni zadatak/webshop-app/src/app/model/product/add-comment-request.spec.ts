import { AddCommentRequest } from './add-comment-request';

describe('AddCommentRequest', () => {
  it('should create an instance', () => {
    expect(new AddCommentRequest()).toBeTruthy();
  });
});
