import { GetAllImagesResponse } from './get-all-images-response.model';

describe('GetAllImagesResponse', () => {
  it('should create an instance', () => {
    expect(new GetAllImagesResponse()).toBeTruthy();
  });
});
